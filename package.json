{
  "name": "temp_serv",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "./monitor.sh ",
    "bundle": "esbuild index.js --bundle --format=esm --outdir=./bundle",
    "serve": "cp index.html ./bundle/ && esbuild index.js --bundle --format=esm --outdir=./bundle --servedir=./bundle"
  },
  "author": "DrA1ex",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "chart.js": "^4.4.0",
    "lowdb": "^6.1.1",
    "telegraf": "^4.15.0",
    "text-graph.js": "^1.1.2",
    "ultimate-text-to-image": "^1.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.19.5"
  }
}
